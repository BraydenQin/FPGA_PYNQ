# 基于PYNQ的大模型量化推理加速器项目三人分工方案
 
算法与硬件逻辑开发
 
负责硬件加速器的算法落地与IP核开发，是PL端（FPGA）核心开发主体。
 
1. 基于 llama2.c  /  llama.cpp 框架，完成INT8/INT16量化矩阵乘加（MAC）算法的C/C++实现，嵌入移位缩放（Bit-shift）逻辑替代浮点运算；
2. 针对矩阵计算循环，设计循环展开（Loop Unroll）的优化策略，标注Vitis HLS编译指令；
3. 使用Vitis HLS将优化后的C/C++代码综合为硬件IP核，完成逻辑仿真、时序分析，确保算力与资源占用平衡；
- 输出IP核文档（接口定义、时钟/资源消耗），提供给硬件系统搭建工程师。
 
 
 
硬件系统搭建与集成
 
负责硬件系统的整体搭建、总线互联与比特流生成，衔接硬件IP与板级部署。
 
1. 在Vivado中导入ZYNQ核心、CDMA模块、算法工程师生成的加速器IP，通过AXI总线（AXI Full/AXI Lite）完成各模块的互联；
2. 完成时钟、复位、DDR控制器等基础外设的配置，进行硬件系统的综合、实现与布局布线；
3. 调试硬件系统的时序约束，解决总线冲突、资源占用超限等问题；
4. 生成Bitstream文件，导出XSA文件（供Vitis SDK使用）和硬件覆盖层（Overlay，供PYNQ使用）。
 
 
 
软件驱动与全流程验证【我负责的部分】
 
负责软件层（PS端）的驱动开发、推理框架适配与全流程测试，是软硬件协同的关键衔接者。
 
- 可选方案A（PYNQ）：基于Jupyter Notebook编写Python驱动，实现DMA数据搬移、AXI Lite配置/启动指令发送、结果回传逻辑；
- 可选方案B（Vitis SDK）：基于XSA文件和BSP包，编写C语言独立应用程序，封装硬件控制的API接口。
 
1. 基于 llama2.c  /  llama.cpp 框架，集成硬件加速的矩阵乘法模块，替换原有浮点矩阵计算逻辑；
2. 完成端到端测试：量化数据输入→硬件加速计算→移位缩放反量化→结果验证，对比纯软件推理的精度/性能。
